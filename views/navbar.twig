{% if is_granted('IS_AUTHENTICATED_FULLY') %}
<div class="navbar">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>            
            <div class="nav-collapse">
                <ul class="nav">                  
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">User <b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{path("user.list")}}">List</a></li>
                                <li><a href="{{ app.baseUrl }}user/add">Add</a></li>                            
                            </ul>
                        </li>    
                    {% endif %}                        
                    </ul>    
                    <ul class="nav pull-right">
                        <li class="divider-vertical"></li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ app.security.getToken().getUser().getUsername() }} <b class="caret"></b></a>
                            <ul class="dropdown-menu">                               
                                <li><a href="{{ app.baseUrl }}logout">Logout</a></li>                           
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endif %}
